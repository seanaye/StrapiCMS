{
  "host": "${(process.env.NODE_ENV === 'development') ? 'localhost' : 'cms.seanaye.ca'}",
  "port": "${(process.env.NODE_ENV === 'development') ? 1337 : 443}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
